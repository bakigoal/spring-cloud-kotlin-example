management:
  health:
    db:
      enabled: false
    diskspace:
      enabled: true
  endpoint:
    health:
      enabled: true
      show-details: always
  endpoints:
    web:
      base-path: /
    enabled-by-default: false